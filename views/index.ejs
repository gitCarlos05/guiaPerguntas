<%- include('partials/header') %>

    <body>
        <nav class="navbar navbar-dark bg-primary">
            <a class="navbar-brand" href="/perguntas"><img src="/imagens/guia.png" alt="GuiaProgramador"></a>
        </nav>
        <div class="container">
            <hr>
            <h1>Perguntas</h1>
            <hr>
            <a href="/perguntas" class="btn btn-primary">Perguntar</a>
            <hr>
            <br>
            <%perguntas.forEach(pergunta=> {%>
                <br>
                <div class="card">
                    <div class="card-body">
                        <h3>
                            <%=pergunta.titulo%>
                        </h3>
                    </div>
                    <div class="card-footer">
                        <button class="btn btn-primary">Responder
                        </button>
                        <!-- <%=pergunta.descricao%> -->
                    </div>


                </div>
                <% });%>
        </div>
    </body>

    <%- include('partials/footer') %>